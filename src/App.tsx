/**
 * Legacy entry - not used by Next.js.
 * The app uses the Next.js App Router (app/ directory).
 */
const App = () => (
  <div className="p-8 text-center text-muted-foreground">
    This project runs on Next.js. Use <code>npm run dev</code> and open the app URL.
  </div>
);

export default App;
